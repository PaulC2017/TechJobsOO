@{
    // TODO #2.1- declare the ViewModel (outside of this @{ } block)
    //TODO #2.1 Completed

    ViewData["Title"] = "View Job";
}
@model TechJobs.Models.Job 

<table class="job-listing">
    @{
        // TODO #2.2 - display the job fields, one per row (outside of this @{ } block)
        //TODO #2.1 Completed
    }

    <tr><td>Name</td><td>@Model.Name</td></tr>
    <tr><td>Employer</td><td>@Model.Employer</td></tr>
    <tr><td>Location</td><td>@Model.Location</td></tr>
    <tr><td>Core Competency</td><td>@Model.CoreCompetency</td></tr>
    <tr><td>Position Type</td><td>@Model.PositionType</td></tr>

</table>